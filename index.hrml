<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8" />
<title>GrievanceBee – Investor Briefing SPA</title>
<meta name="viewport" content="width=device-width, initial-scale=1.0" />
<style>
    /* ========= GLOBAL DESIGN THEME ========= */
    :root {
        --bg: #0d0d17;
        --card-bg: #151525;
        --accent: #ffd000;
        --accent-bright: #ffef5a;
        --text: #f0f0f5;
        --text-soft: #b6b6c8;
        --bee-orange: #ffb300;
        --bee-yellow: #ffdd33;
        --bee-gradient: linear-gradient(135deg, #ffb300, #ffe55b);
        --radius: 12px;
        --shadow: 0 4px 20px rgba(0,0,0,0.35);
    }
    body {
        margin: 0;
        background: var(--bg);
        color: var(--text);
        font-family: "Inter", system-ui, sans-serif;
        -webkit-font-smoothing: antialiased;
    }
    h1, h2, h3 { margin: 0; }

    /* ========= NAVIGATION ========= */
    nav {
        position: sticky;
        top: 0;
        background: rgba(13,13,23,0.9);
        backdrop-filter: blur(10px);
        padding: 18px 24px;
        display: flex;
        justify-content: space-between;
        align-items: center;
        border-bottom: 1px solid rgba(255,255,255,0.07);
        z-index: 100;
    }
    nav .logo {
        font-size: 26px;
        font-weight: 800;
        background: var(--bee-gradient);
        -webkit-background-clip: text;
        color: transparent;
    }
    nav button {
        background: var(--bee-gradient);
        border: none;
        padding: 10px 20px;
        border-radius: var(--radius);
        font-weight: 600;
        cursor: pointer;
        box-shadow: var(--shadow);
        transition: 0.3s;
    }
    nav button:hover { transform: translateY(-2px); }

    /* ========= SECTION STYLING ========= */
    section {
        padding: 70px 20px;
        max-width: 1200px;
        margin: auto;
    }
    .hero {
        text-align: center;
        padding-top: 100px;
        padding-bottom: 100px;
    }
    .hero h1 {
        font-size: 55px;
        font-weight: 900;
        background: var(--bee-gradient);
        -webkit-background-clip: text;
        color: transparent;
    }
    .hero p {
        margin-top: 18px;
        font-size: 20px;
        color: var(--text-soft);
        max-width: 720px;
        margin-left: auto;
        margin-right: auto;
    }

    /* ========= CARD GRID ========= */
    .grid {
        display: grid;
        gap: 26px;
        grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
        padding: 30px 0;
    }
    .card {
        background: var(--card-bg);
        padding: 26px;
        border-radius: var(--radius);
        box-shadow: var(--shadow);
        transition: 0.3s;
    }
    .card:hover {
        transform: translateY(-5px);
        box-shadow: 0 6px 25px rgba(255, 223, 80, 0.25);
    }
    .card h3 {
        font-size: 22px;
        color: var(--accent-bright);
        margin-bottom: 10px;
    }
    .card p {
        color: var(--text-soft);
        line-height: 1.45;
    }

    /* ========= CTA SECTION ========= */
    .cta {
        text-align: center;
        margin-top: 80px;
    }
    .cta button {
        background: var(--bee-gradient);
        padding: 18px 36px;
        border-radius: var(--radius);
        border: none;
        font-size: 20px;
        font-weight: 700;
        cursor: pointer;
        box-shadow: var(--shadow);
        transition: 0.3s;
    }
    .cta button:hover { transform: scale(1.05); }

    /* ========= FOOTER ========= */
    footer {
        margin-top: 90px;
        padding: 40px 20px;
        text-align: center;
        color: var(--text-soft);
        font-size: 14px;
    }
</style>
</head>
<body>

<!-- ================= NAV ================= -->
<nav>
    <div class="logo">GrievanceBee</div>
    <button onclick="scrollToSection('cta')">Invest Now</button>
</nav>

<!-- ================= HERO ================= -->
<section class="hero">
    <h1>The Future of Justice Is a Single File</h1>
    <p>
        GrievanceBee transforms chaotic, traumatic, undocumented injustices into 
        organized, court-ready records — instantly, offline, and without requiring 
        legal knowledge, money, or internet access.
    </p>
</section>

<!-- ================= WHAT IS GRIEVANCEBEE ================= -->
<section>
    <h2 style="text-align:center;color:var(--accent-bright);font-size:34px;font-weight:800;">
        What Is GrievanceBee?
    </h2>

    <div class="grid">
        <div class="card">
            <h3>⚡ SPA Generator Engine</h3>
            <p>
                A single HTML file that builds apps — SPAs that create SPAs.  
                No backend. Zero dependency. Pure offline power.
            </p>
        </div>
        <div class="card">
            <h3>🛡️ Civil Rights Evidence Kit</h3>
            <p>
                Built from our 300-violation lexicon.  
                Captures abuse, misconduct, and denial-of-care with timestamps, witnesses, and exportable reports.
            </p>
        </div>
        <div class="card">
            <h3>💊 Medical Neglect Tracker</h3>
            <p>
                Logs missed medications, symptoms, vitals and health deterioration 
                — generating medical-legal documentation automatically.
            </p>
        </div>
        <div class="card">
            <h3>📈 SMB Micro-App Studio</h3>
            <p>
                Generates CRMs, schedulers, intake forms, invoices, and dashboards 
                for millions of small businesses — instantly.
            </p>
        </div>
    </div>
</section>

<!-- ================= MARKET & IMPACT ================= -->
<section>
    <h2 style="text-align:center;color:var(--accent-bright);font-size:34px;font-weight:800;">
        Market, Momentum & Mission
    </h2>

    <div class="grid">
        <div class="card">
            <h3>🌍 Massive Global Need</h3>
            <p>
                Billions lack legal knowledge, resources, or tools to document injustice.  
                We give them evidence, structure, and empowerment in <b>one file</b>.
            </p>
        </div>
        <div class="card">
            <h3>📊 Multi-Billion TAM</h3>
            <p>
                Legal Tech • Civil Rights Tech • Medical Advocacy •  
                SMB Automation — all converging into a single evolutionary platform.
            </p>
        </div>
        <div class="card">
            <h3>🧠 AI-Optional = Future-Proof</h3>
            <p>
                Works with or without AI. Works with or without internet.  
                A resilient, humanitarian, universally deployable platform.
            </p>
        </div>
        <div class="card">
            <h3>🚀 Passive Revenue Ready</h3>
            <p>
                Template packs, licensing, NGO partners, SMB microtools,  
                donation flows and auto-generated mini-app storefronts.
            </p>
        </div>
    </div>
</section>

<!-- ================= CTA ================= -->
<section class="cta" id="cta">
    <h2 style="font-size:40px;font-weight:900;background:var(--bee-gradient);-webkit-background-clip:text;color:transparent;">
        Build the Justice Platform of the Future ✦
    </h2>
    <p style="color:var(--text-soft);max-width:700px;margin:auto;margin-top:12px;">
        Join us as we expand the world’s first offline, AI-optional, generative justice engine — 
        a tool that empowers people facing police violence, homelessness, incarceration, discrimination, 
        medical neglect, and systemic abuse.
    </p>

    <button style="margin-top:40px;" onclick="alert('Investor deck will be sent shortly!')">
        Request Investor Deck
    </button>
</section>

<!-- ================= FOOTER ================= -->
<footer>
    © 2025 GrievanceBee • Justice. Evidence. Dignity. Delivered Offline.
</footer>

<script>
function scrollToSection(id) {
    document.getElementById(id).scrollIntoView({ behavior: 'smooth' });
}
</script>

</body>
</html>